{
  "skill_confirm_timeout": {
    "description": "Skill 命令确认等待时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "报告返回后等待“确认”的秒数，超时不安装。"
  },
  "skill_review_provider_id": {
    "description": "Skill 审查模型（可选）",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "留空使用主模型；指定后，命令直接返回该模型的报告。"
  },
  "skill_scan_mode": {
    "description": "Skill 静态扫描策略",
    "type": "string",
    "default": "enforce",
    "options": ["enforce", "report_only"],
    "hint": "AI 工具的扫描策略：enforce 拦截风险，report_only 仅报告。"
  },
  "diff_mode": {
    "description": "编辑模式",
    "type": "bool",
    "default": true,
    "hint": "使用差异替换编辑 MCP 配置，避免整份覆盖。"
  },
  "diff_match_threshold": {
    "description": "文本匹配阈值",
    "type": "int",
    "default": 100,
    "hint": "差异替换的文本匹配度；100 为完全匹配。",
    "slider": {
      "min": 50,
      "max": 100,
      "step": 1
    }
  }
}
